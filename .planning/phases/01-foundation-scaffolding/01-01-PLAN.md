# Plan: 01-01 - Design Token System and Content Configuration

Establishing the foundation for modular theming and content separation.

## Proposed Changes

### Configuration
#### [NEW] `app/config/content.ts`
- Define `Content` interface.
- Export `appContent` object with sections: `hero`, `socialProof`, `registration`, `footer`, `faq`.

### Styling
#### [NEW] `app/styles/tokens.css`
- Core variables: `--font-main`, `--font-accent`, `--color-primary`, `--color-bg`, `--color-text`, `--radius-m`, etc.
- Base values for common UI elements.

#### [NEW] `app/styles/themes/v1.css`
- SpinQuest theme overrides (blue/neon).

#### [NEW] `app/styles/themes/v2.css`
- Luck Is a Journey overrides (gold/cinematic).

#### [UPDATE] `app/globals.css`
- Import `tokens.css`.
- Set base body styles.

## Verification Plan

### Automated Tests
- N/A for this phase (scaffolding).

### Manual Verification
- Check that changing code in `layout.tsx` to set `data-theme="v2"` correctly swaps colors/fonts.
- Verify that text in components is correctly pulled from `content.ts`.
