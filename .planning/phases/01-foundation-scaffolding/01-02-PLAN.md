# Plan: 01-02 - Root Layout and Header Component

Building the common layout that persists across themes.

## Proposed Changes

### Components
#### [NEW] `app/components/layout/Header.tsx`
- Logo placeholder (SpinQuest/Compass).
- Login / Sign Up buttons.
- Sticky positioning as per TZ.

#### [NEW] `app/components/layout/StickyBar.tsx`
- Top promo bar with "CLAIM OFFER".

### Layout
#### [UPDATE] `app/layout.tsx`
- Integrate `StickyBar` and `Header`.
- Add theme switcher logic (via URL query or ENV).
- Setup fonts via `next/font`.

## Verification Plan

### Automated Tests
- N/A.

### Manual Verification
- Page renders with StickyBar and Header.
- Buttons are styled according to tokens.
- Sticky logic works when scrolling (will test in Phase 2).
